{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "PORT=$(node -p \"require('./config.js').PORT\") PRIVATE_KEY=$(node -p \"require('./config.js').PRIVATE_KEY\") node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^4.1.3",
    "socket.io-encrypt": "file:../.."
  }
}
